<template>
  <div class="w-full h-screen mx-auto">
    <!-- <div v-for="{ id, title, content } in posts" :key="id">{{ id }} - {{ title }} - {{ content }}</div> -->
    <Form />
    <Feature />
  </div>
</template>

<script lang="ts" setup>
/* const { data: posts } = await useAsyncData("api", () => $fetch("/api/post")); */
</script>
